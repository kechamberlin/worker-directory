# worker-directory

This is a webpage built via react that displays an employee directory (the data of which is stored in a json file).

Each employee card displays a name, occupation, and location.

The user has the ability to deleted an employee card by clicking the red "x" in the top right corner.